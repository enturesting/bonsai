# Web
fastapi==0.115.5
uvicorn[standard]==0.32.1
sse-starlette==2.1.3
jinja2==3.1.4
aiofiles==24.1.0
httpx==0.27.2
python-multipart==0.0.9

# Anthropic (grower Opus 4.8 + checker Haiku 4.5)
# 0.40.0 was pre-structured-outputs: no messages.parse(), no output_config/thinking on create().
# 0.112.0 has all three (verified vs claude-api skill). httpx/pydantic bounds are wide → no conflict.
anthropic==0.112.0

# MongoDB Atlas + async driver (motor 3.6 needs pymongo >=4.9,<4.10; dnspython for mongodb+srv://)
pymongo==4.9.2
motor==3.6.0
dnspython==2.7.0

# Voyage embeddings
voyageai==0.3.2

# Gemini AUT (swap to google-genai if Gemini 3.5 / Antigravity needs the newer SDK)
google-generativeai==0.8.3

# Utilities
python-dotenv==1.0.1
numpy==1.26.4
scipy==1.13.1
pydantic==2.9.2
pydantic-settings==2.5.2

# Test
pytest==8.3.4
pytest-asyncio==1.2.0  # async seed_failures tests (fixtures)
