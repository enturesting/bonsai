[pytest]
asyncio_mode = auto
testpaths = store/tests
markers =
    atlas_it: real MongoDB Atlas + Voyage integration test (needs RUN_ATLAS_IT=1)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
